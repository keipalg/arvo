/**
 * Constants file to list any costing/pricing/expense related constants
 * Note:
 * - % Constants should be in their raw decimal form for ease of calculation
 * and have the frontend side take care of formatting percentage.
 */

// For Pricing - COGS
export const DEFAULT_OVERHEAD_COST_PCT: number = 0.2;
export const DEFAULT_LABOR_COST_PCT: number = 0.5;
// For Pricing - Operating Cost
export const DEFAULT_OPERATING_COST_PCT: number = 0.5;
// For Pricing - Sale Price
export const DEFAULT_PROFIT_MARGIN_PCT: number = 0.25;
